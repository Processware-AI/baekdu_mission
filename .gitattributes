# 기본: 저장소에는 LF로 보관
* text=auto eol=lf

# Windows 배치 파일은 반드시 CRLF (LF면 cmd.exe가 실행하지 못함)
*.bat  text eol=crlf

# macOS / 셸 스크립트는 반드시 LF
*.command text eol=lf
*.sh      text eol=lf

# 바이너리
*.png binary
*.jpg binary
*.mp4 binary
